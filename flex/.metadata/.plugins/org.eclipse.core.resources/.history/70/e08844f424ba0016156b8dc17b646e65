package com.greensock.loading {

    public class YoutubeLoader extends VideoLoader {
        public function YoutubeLoader(urlOrRequest:*, vars:Object = null) {
            super(urlOrRequest, vars);
        }
    }
}
