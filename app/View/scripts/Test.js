angular.module('TestApp', ['MinuteFramework'])
    .controller('TestController', ['$scope', '$minute', function($scope, $minute) {
        $scope.init = function() {
            //TODO create controller code
        };
    }]);
